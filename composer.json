{
    "require-dev": {
        "lucatume/wp-browser": "^1.21"
    },
    "require": {
        "jakoch/phantomjs-installer": "^2.1",
        "site5/phantoman": "^2.0"
    },
    "config": {
        "bin-dir": "bin"
    },
    "scripts": {
        "post-install-cmd": [
            "PhantomInstaller\\Installer::installPhantomJS"
        ],
        "post-update-cmd": [
            "PhantomInstaller\\Installer::installPhantomJS"
        ]
    }
}
